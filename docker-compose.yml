version: '3'
services:
  postgres:
    image: postgres:10
    restart: always
    container_name: postgres-drg
    env_file:
      - .env
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
